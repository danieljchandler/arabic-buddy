# App audit — 2026-09-18

Whole-app pass for "what is not working", taken on `main` at `89f6caf`
(the merge of #379). Evidence was gathered on 2026-09-18 from four places:
the repository, this repo's own CI history on GitHub, the **live** Supabase
project (`ovscskaijvclaxelkdyf`) probed as the anon role, and the published
frontend at `laha-arabic.lovable.app`.

**Nothing has been fixed.** Every item names the layer that owns it —
**deploy**, **edge function**, **RLS**, **storage**, **frontend**,
**domain logic**, **content** or **guard** — and is marked CONFIRMED (the
code or the live response was read) or SUSPECTED (reasoned, not reproduced).

The finding that organises all the others: **every automated gate this repo
has is green, and almost nothing below is the kind of thing they can see.**
The four CI jobs test the repository. They do not test the deployment, they
do not evaluate RLS semantics, several guards check shape rather than
behaviour, and none of them can see production data.

## Automated checks

All run locally against `89f6caf` unless noted.

| Check | Result |
| --- | --- |
| `npm run typecheck` (app + e2e tsconfigs) | pass |
| `npm run lint:ratchet` | pass — 526 errors, 49 warnings, exactly at baseline |
| `npm test` (vitest) | pass — 387 files, 6550 tests, 5 skipped, 207s |
| `npm run build` | pass — 22.5s |
| `npm run check:edge` (deno check) | pass |
| `npm run test:edge` (deno test) | pass — 2063 tests, 0 failed, 4m |
| `npm run test:e2e` (Playwright) | pass — 2172 tests, Chromium, 24.8 min |
| Migration replay | **not run** — no Postgres server in this environment; green in CI on `89f6caf` |
| CI on `main` (run 517) | all four jobs green |

## What was and was not exercised

| Check | Done | Notes |
| --- | --- | --- |
| Static read of `src/`, `supabase/functions/`, `supabase/migrations/` | yes | five parallel agents, one per domain; every finding re-read before it was kept |
| CI deploy history | yes | all 234 `main` push runs, 2026-07-31 → 2026-09-18, job logs fetched and parsed |
| Live edge functions, reachability | yes | all 119 deployed functions POSTed with the publishable key |
| Live tables, anon role | partial | 13 tables; anon cannot see `authenticated`-only tables, so those are unknown, not empty |
| Live write privileges | partial | anon INSERT probe on the three write-asymmetric tables (see B4); no authenticated session available |
| Published frontend | yes | HTML and JS bundle fetched and compared against the repo |
| DNS for the canonical domain | yes | Cloudflare DoH |
| Live RLS as a **learner** | **no** | needs a confirmed account; several items below stay SUSPECTED for this reason |
| Paid pipelines end to end (TTS, ASR, LLM spend) | **no** | one incidental live LLM call, see B1 |
| Authenticated crawl of every route | **no** | the 2026-09-04 crawl in `qa/output/` is the most recent, and predates 15 migrations |

---

## A. Production is running older code than this repository

This is the spine of the audit. Two independent delivery gaps — edge
functions and the frontend — plus the marker that was built to detect the
first one, currently blind.

### A1 — CI has never once deployed an edge function — deploy — CONFIRMED

The `Deploy edge functions` job (`.github/workflows/ci.yml:272`) has run on
every `main` push since the workflow was added. I fetched and parsed the job
log for all **234** such runs, 2026-07-31 to 2026-09-18:

- runs where the deploy step actually ran: **0**
- runs that logged "these edge functions were NOT deployed": **27**
- distinct functions named across those: **81**

`SUPABASE_ACCESS_TOKEN` has never been set, so `ci.yml:350` takes the skip
branch every time. The job's own comment explains why it is a warning rather
than a failure, and that reasoning is sound — a Lovable Cloud project cannot
mint the token, and a permanently red `main` would be worse. But the
consequence is that **the automated path has never worked at all**, which is
a different thing from "it occasionally skips".

The most recent merge is a live example: #379 ("Let a transcriber correct a
video's title") left `transcript-review` and `resync-transcript-timing`
behind. The 81 also include `create-checkout`, `customer-portal`,
`check-subscription`, `access-credentials` and `process-approved-video`.

### A2 — Functions do reach production by hand, but not reliably current — deploy — CONFIRMED

Correcting an assumption that A1 invites: I POSTed all 119 deployed function
names against the live project. **Zero 404s.** Distribution: 96×401, 13×403,
7×400, 2×500, 1×200. So someone deploys them out of band, and
`QA_MAP.md`'s `persist-video-thumbnail` **NOT DEPLOYED (404)** annotation —
repeated on roughly nine routes — is **stale**; it now answers 403.

What is *not* current is the code inside them. `customer-portal` live returns:

```
500 {"error":"Authentication error: invalid claim: missing sub claim"}
```

An auth failure as a 500. `docs/quality-audit-2026-08-27.md` records this
class (G4) as fixed for `create-checkout`, which now returns a 500 of its own
on a *validation* error (`{"error":"Invalid tier: undefined…"}`). The
deployed billing code is behind the repository, on the one path where a
wrong status code reaches a payment provider.

### A3 — The staleness banner is blind right now — edge function / frontend — CONFIRMED

`EdgeBuildBanner` exists precisely to catch A1/A2, and `edgeBuild.ts`'s own
docstring warns: "**Bump this whenever an edge function changes** … Leaving
it alone is what makes the check quietly stop working." That is the current
state.

`EDGE_BUILD = "2026-09-16.1"` (`supabase/functions/_shared/edgeBuild.ts:23`),
last bumped at `a5f38fc`. Since then these edge files changed on `main`:

```
_shared/arabicReviewCore.ts
_shared/transcriptRevisionCore.ts
analyze-gulf-arabic/index.ts
assistant-chat/index.ts
transcript-review/index.ts
```

`analyze-gulf-arabic` is one of the two `PIPELINE_FUNCTIONS` the banner
watches (`src/lib/edgeBuildStatus.ts:85`). Both halves import the same
unbumped constant, so the probe compares `2026-09-16.1` against
`2026-09-16.1` and reports **current** while the backend is behind.

Two further limits on the same mechanism, independent of the bump:

- it probes **one** function (`useEdgeBuildStatus.ts:32`) as a proxy for all
  121, so a function deployed out of step with the rest is invisible;
- it renders only on `/admin/videos` and the video edit page, behind
  `canManage` — so a `transcriber`, the exact role #379 was built for, can
  never see it.

### A4 — The published frontend predates the brand rename — deploy — CONFIRMED

`https://laha-arabic.lovable.app` serves a `<title>` still carrying the
**pre-rename brand spelling**, and its `index-5yWMYFPn.js` bundle contains
**zero** occurrences of `Hikaya`. The rename landed at `2488e4d` on
**2026-09-07**, so the published bundle is at least eleven days old.

(The old spelling is not quoted literally here on purpose — `brandSpelling`
scans every tracked text file, this document included, and an audit note is
not one of the places where the old name is still correct.)

### A5 — The canonical domain does not resolve — frontend — CONFIRMED

`index.html:25` (`<link rel="canonical">`) and `:30` (`og:url`) both point at
`https://hakiya.app/`. Cloudflare DoH returns **Status 3 (NXDOMAIN)** for
`hakiya.app`. `hikaya.app` **is** registered (SOA, DigitalOcean nameservers).

Three hosts are therefore in play at once:

| Names | Says |
| --- | --- |
| `index.html` canonical + `og:url` | `hakiya.app` — does not exist |
| `public/sitemap.xml` (26 URLs), `public/robots.txt` | `laha-arabic.lovable.app` |
| DNS | `hikaya.app` is the registered domain |

Every share card and every search-engine canonical points at a dead host;
the sitemap points somewhere else again. `_shared/cors.ts:12` also puts
`hakiya.app` first in `DEFAULT_ORIGINS`, which makes it
`getProductionOrigin()`'s answer — though the blast radius there is small,
because both call sites (`create-checkout:107`, `customer-portal:50`) prefer
the request's `Origin` header and only fall back.

Note for whoever fixes this: `brandSpelling`'s allow-list explicitly blesses
"the `hakiya.app` domain" as a place the old spelling is still correct. That
exemption is now wrong, and the guard will defend the dead domain.

### A6 — A migration merged yesterday is almost certainly unapplied — deploy / RLS — CONFIRMED (repo side), SUSPECTED (live side)

`supabase/migrations/20260918090000_transcript_title_revisions.sql:20-29`
drops and re-adds `transcript_line_revisions_field_check` so the closed set
admits `'title'` and `'title_arabic'`. It is hand-written, not uuid-named,
and the last Lovable regeneration is `a1beedb` (2026-09-13). The precedent
migration of identical shape, `20260824130000_dialect_subvarieties.sql`,
*does* have a uuid-named counterpart (`20260825064609_f4532bdf-…sql:19`);
this one does not.

If the CHECK is still the old one in production, the failure is worse than
"the title is not logged". `transcript-review/index.ts:144` inserts a save's
revisions as **one batch**, so a single rejected row drops the audit trail
for *every* field in that save — dialect, sub-variety, cultural context
included — while `index.ts:656` has already written the video row, so the
page reports success. Only `logged:false` and a `console.error` record it.

This is the class `typesDrift` structurally cannot see: no column changes, so
there is nothing for the generated types to disagree about.

---

## B. Security and spend

### B1 — `placement-quiz` serves a generated quiz to an unauthenticated caller — edge function — CONFIRMED (live)

POSTed with nothing but the publishable anon key — which ships in the public
JS bundle — and an empty body, the live function returned a complete
generated quiz:

```
{"questions":[{"question_english":"Listen to the spoken Gulf Arabic phrase…
```

This is deliberate (the placement quiz is offered to visitors) and it is
rate-limited by `enforceAnonymousDailyCap`. But the cap keys on IP, so the
ceiling is per-IP rather than per-caller, and the endpoint is a paid LLM call
reachable by anyone on the internet. Worth a decision on the ceiling rather
than a code fix.

### B2 — Three paid functions have no daily cap at all — edge function — CONFIRMED

None of the three imports `usageCap.ts`:

| Function | Reachable from | Note |
| --- | --- | --- |
| `analyze-gulf-arabic` | `/learn-from-x` | the heaviest pipeline in the app — multi-drafter ensemble, Arabic-native arbitration, Shaheen-MT, vocabulary pass |
| `extract-visual-context` | admin video flow | **pays before it authorizes** |
| `classify-tutor-segments` | `/tutor-upload` | unbounded caller-supplied `segments[]`/`words[]` interpolated into the prompt |

`analyze-gulf-arabic` is authenticated and role-gates the `videoId` *write*
path, but its own comment at `index.ts:2178` states the intent plainly —
"Analysing a transcript is something any learner may do" — so any signed-in
learner can run the full ensemble on an arbitrarily long transcript, as often
as they like. `download-media` carries a comment saying it used to be the
largest uncounted spend and was capped for exactly this reason; these three
did not get the same treatment.

`extract-visual-context` has an ordering bug on top: the Gemini multi-frame
vision call is `index.ts:139`, the `is_admin` check is `index.ts:187`. A
caller who will be refused still costs the project the whole vision call.

`bible-passage` is a fourth, milder case: authenticated and access-checked,
no daily cap.

### B3 — `tutor-audio-clips` and `meme-uploads` are public and enumerable — storage — CONFIRMED

`20260529122406_84f88330-…sql:64-67` grants `anon, authenticated` a
`storage.objects` SELECT covering
`('avatars','meme-uploads','flashcard-images','flashcard-audio','tutor-audio-clips','audio')`,
and `20260209171143_…sql:54` creates `tutor-audio-clips` with `public = true`.
Objects are written at `<auth.uid()>/<file>` (`useTutorUpload.ts:59-78`).

A SELECT policy on `storage.objects` is what the Storage list API checks, so
with the publishable key alone an unauthenticated caller can `list('')` to
enumerate user-id folders, `list('<uid>')` for filenames, and fetch each
object. The contents are learners' recorded tutoring sessions — private
conversation audio — and their uploaded memes/screenshots, keyed to a stable
user id. Both migrations are Lovable-authored, so this is the live state.

### B4 — `user_concept_mastery` is client-writable — RLS — CONFIRMED (mechanism), SUSPECTED (exploitable today)

CLAUDE.md names this one of the three tables where "clients read their own
rows under RLS but writes go through edge functions under the service role,
so nobody can post themselves a score".

`20260731120000_concept_mastery_hardening.sql:62-75` recreates
`"Users insert own mastery"` (INSERT, `WITH CHECK auth.uid() = user_id`) and
`"Users update own mastery"`, then at `:84-89` comments that writes all go
through the service role and issues `GRANT SELECT` — **without a REVOKE**.
Compare `learner_errors`, where the author did revoke
(`20260726140000_owned_table_constraints.sql:52-53`).

I probed the live project to settle the half that was uncertain. An anon
INSERT of `{}` into each of the three tables returns:

```
42501  new row violates row-level security policy for table "user_concept_mastery"
42501  new row violates row-level security policy for table "learner_errors"
42501  new row violates row-level security policy for table "learner_ai_memory"
```

All three reach the **policy** check rather than being refused at the
privilege check ("permission denied for table …"), which confirms the INSERT
privilege is granted on all three. `learner_errors` and `learner_ai_memory`
are still safe because they have no INSERT policy at all, so the check can
never pass. `user_concept_mastery` has a permissive own-row one. Anon fails
only because `auth.uid()` is null; an authenticated learner would satisfy it.

Remaining uncertainty is narrow — whether some out-of-band change revoked it
since — and one authenticated INSERT would close it. Note that
`contract/prelude.sql` carries no `ALTER DEFAULT PRIVILEGES`, so the replay,
`qa/output/schema/rls-tables.txt` and QA_MAP's grant column are all blind to
this whole class.

### B5 — Validation and auth failures returned as HTTP 500 — edge function — CONFIRMED (live)

`create-checkout` → `500 {"error":"Invalid tier: undefined…"}` (a 400).
`customer-portal` → `500 {"error":"Authentication error: invalid claim…"}`
(a 401). Both live-verified; see A2 for why this is also a deploy symptom.

### B6 — react-router advisory affects the production bundle — frontend — CONFIRMED

`npm audit` reports 11 advisories, 4 in production dependencies. The one that
matters at runtime is react-router's open redirect via a backslash in
`<Link>`/`useNavigate` (GHSA-wrjc-x8rr-h8h6); `package.json:74` pins
`^6.30.1` and the fix is the v7 major. `docs/security-review-2026-08.md`
already lists this as its own open item. The `vite` path-traversal
advisories are dev-server only.

---

## C. Features that are silently dead

| # | Feature | Cause | Evidence | Layer |
| --- | --- | --- | --- | --- |
| C1 | **`/leaderboard` returns only you** | `leaderboard_profiles` was created `WITH (security_invoker = false)` precisely to bypass `profiles`' own-row RLS; three minutes later another migration set it back to `true`, almost certainly to silence Supabase's SECURITY DEFINER linter | `20260529150401_…sql:11-17` vs `20260529154932_1e521bcb-…sql:1`; `useLeaderboard.ts:65-72` returns `[]` when the view is empty | RLS |
| C2 | **Friends' streaks and XP always 0** | three policies gate on `EXISTS (SELECT 1 FROM profiles WHERE … show_on_leaderboard)`, but `profiles` is readable own-row-only, so the `EXISTS` can never see another user's row and each policy collapses to "own rows" | `rls-policies.txt:273,244`; `20260904120000_…sql:107-116` — which was *written* to fix friends' streaks by mirroring the `user_xp` rule | RLS |
| C3 | **Weekly goal choice discarded** | three independent faults, any one of which is enough — see below | `20260529150401_…sql:166-167`; `Onboarding.tsx:178-185`, `Settings.tsx:386`; `useGamification.ts:160-166` | RLS + frontend |
| C4 | **`/analytics` accuracy panel** | reads `user_vocabulary.correct_count` / `review_count`, which **nothing in the repo writes** — they appear to be pre-FSRS legacy columns, and they exist in `types.ts` but in no migration | `useAnalytics.ts:79,86,125,178,223,237` | frontend + schema |
| C5 | **Mark-unknowns on `/reading-library/:id`** | provider and save bar are mounted, the toggle never is, and nothing calls `setEnabled` — so marking mode cannot be entered and the bar (which returns null while empty) can never appear | `ReadingLibraryStory.tsx:248,418`; cf. `ReadingPractice.tsx:1097` | frontend |
| C6 | **Sign-up dead-ends and signs the user out** | `useAuth.signUp` discards `data`, so with email confirmation ON there is no session; `redeem_invite_code` (which raises `28000` when `auth.uid()` is null) then fails, and the handler responds by calling `signOut()` and showing "Invite code couldn't be redeemed" | `useAuth.ts:43-54`, `Auth.tsx:125-152`; RPC at `20260628201424_…sql:66-69` | frontend |
| C7 | **A flaky connection reads as "Access Denied"** | `useAdminAuth` runs four `has_role` RPCs and reads only `.data`, never `.error`; postgrest returns transport failures as `{data: null, error}` rather than throwing, so the `catch` never fires and failure resolves to "no roles" | `useAdminAuth.ts:76-92`; toast at `AdminLayout.tsx:30-48` | frontend |

C6 is the most severe of these for a beta: the account *is* created, so the
learner cannot retry with the same email, and the invite is never recorded.
It is CONFIRMED in code and conditional on the project's email-confirmation
setting, which `QA_MAP.md` §Flows 1 states is on.

**C3 in detail**, because the same migration both caused it and shows the
intended design. `20260529150401_…sql` §5 is titled "weekly_goals: remove
client writes, add `increment_review_count()`". It drops the INSERT and
UPDATE policies and adds a `SECURITY DEFINER` RPC — the write-asymmetric
pattern CLAUDE.md describes, done correctly. What was missed is that the two
client call sites still `upsert` directly, and neither destructures the
result, so the refusal is invisible and both surfaces report success
("Welcome to Hikaya! 🎉", "Settings saved!").

Underneath that sit **three different week keys** for one table:

| Who | Key | Writes |
| --- | --- | --- |
| `increment_review_count()` RPC | `date_trunc('week', …)` — **Monday**, UTC | `completed_reviews` |
| `Onboarding.tsx` / `Settings.tsx` | `getDate() - getDay()` — **Sunday**, UTC | `target_reviews`, `target_xp` |
| `useGamification.ts:160-166` (reader) | Monday, **local** via `localDateKey` | — |

So even with the policies restored, the targets would be written to a row
keyed to a day nothing reads, while progress accumulates on a different row
again. `profiles.weekly_goal` *does* save, which is what hides all of this:
the settings form reads its own value back correctly. Every learner's goal
ring therefore runs against the column defaults (50 reviews / 500 XP)
whatever they chose.

### Content, probed live as anon

These four tables are `public`-readable, so the counts are real:

| Table | Rows visible to anon |
| --- | --- |
| `published_clips` | **0** |
| `daily_challenges` (published) | **0** |
| `set_phrases` (published) | **2** |
| `authentic_stories` / `interactive_stories` (published) | **1** / **1** |

`/clips` and the daily-challenge card on the home feed have nothing to show.
`/set-phrases` — a whole flow with a practice and a review page — has two
published chunks.

**A correction to `docs/qa-audit-2026-09-04.md` B1:** the curriculum *is*
seeded in production now. `lessons` = **54**, `vocabulary_words` = **919**,
`curriculum_stages` = **6**, which matches what the seed migration claims.
The 2026-09-04 note that `/curriculum` shows "No lessons yet" no longer holds.

**Not determinable from outside:** `listen_episodes` and
`curriculum_concepts` have `authenticated`-only SELECT policies, so the zero
the anon probe returns says nothing about them. They need the authenticated
run. I flag this because it is exactly the trap `QA_MAP.md`'s own legend
warns about.

---

## D. Domain-logic defects

### D1 — A review can be scheduled with `intervalDays = 0` — domain logic — CONFIRMED

`spacedRepetition.ts:481` is `newInterval = Math.round(newStability *
intervalFactor)` with **no floor**. The first-exposure branch 30 lines above
(`:448`) *does* have one: `Math.max(rating === 'easy' ? 4 : 1, …)`. The clamp
below only fires `if (newInterval >= 1)` — correctly, because the learning
steps are legitimate sub-day constants — so a rounded-to-zero interval passes
straight through to `nextReviewAt = now + 0ms`.

`retentionFactor(0.97)` is 0.2228, and `desired_retention` is selectable up
to 0.97 in Settings, so any card whose new stability is ≤ ~2.2 days schedules
to zero: most young cards, and every card just out of relearning. The card is
written back due immediately, renders "< 1m" on **Good**, and the session
re-serves it indefinitely. Reachable at the 0.9 default too, for stability
below 0.5 (`MIN_STABILITY` is 0.1).

`spacedRepetition.test.ts:342` exercises `desiredRetention: 0.97` only on a
high-stability card, so the suite never reaches this.

### D2 — No maximum interval — domain logic — CONFIRMED

Same line. `MAX_STABILITY` is 36500 but the *interval* is unbounded:
36500 × `retentionFactor(0.7)` (9.288) × the 1.5 calibration ceiling ≈
**508,000 days**. Anki's 36500-day interval cap has no counterpart here. A
card that reaches it leaves the learner's schedule with no error anywhere.

### D3 — Microphone denial is silent on the SRS card — frontend — CONFIRMED

`PronunciationButton.tsx:121-123` is `catch { console.error(…) }` — no toast,
no state change, no disabled state, so the button looks dead. The identical
bug was found and fixed at the other recorder, and
`PronunciationPractice.tsx:199-206` still carries the comment "Silence here
left the record button looking dead". This call site was missed.

The same component has **no unmount cleanup** for its recorder or
MediaStream (compare `SetPhrasesPractice.tsx:59-66`, which does). Navigating
away or advancing the deck mid-recording leaves the mic indicator lit until
the 5s timeout, and the late `onstop` then scores the *previous* card's word
and lands it as state on the card now on screen.

### D4 — `sseChat` never cancels the reader — domain logic — CONFIRMED

`sseChat.ts:88-155` has no `try`/`finally` around the read loop. On abort,
`reader.read()` rejects, `streamChat` throws, and `finishContent()` at `:153`
is never reached. The module's own docstring (`:38-46`) tells callers to
drive the composer and spinner off `onContentComplete` rather than the
resolved promise — so an aborted reply leaves the composer locked. Neither
the abort path nor the mid-stream `SseChatError` at `:135` releases the lock
or calls `resp.body.cancel()`.

The CLAUDE.md invariant itself holds: the loop reads past `[DONE]`,
post-terminator deltas are dropped, and app frames after it are delivered.

### D5 — `reviewQueue` loses ratings silently — domain logic — CONFIRMED

`reviewQueue.ts:47-69`. `enqueue` returns a fully-formed `QueuedRating` even
when `safeWrite` threw, so the caller reports the rating queued and it is
gone — and an extended offline session is exactly what fills the quota that
causes the throw. One malformed byte in the key discards the learner's
**entire** pending queue rather than one entry. `reviewQueue.test.ts:187-220`
asserts only that these paths do not throw, never that data survived.

### D6 — `useAdminAuth` leaks its auth listener — frontend — CONFIRMED

`useAdminAuth.ts:24-71` assigns `subscription` *inside* an async `init()`,
after `await getSession()`. The effect's cleanup runs synchronously and sees
`null` whenever the component unmounts first — StrictMode's double-mount, or
any fast navigation. The listener is then orphaned and never removed.
`useAuth.ts:11-40` has the correct shape.

---

## E. Guards and documents that have stopped telling the truth

A guard that has quietly stopped covering its subject is worth as much as a
bug, because it is why the next person will not look.

| # | What | Reality |
| --- | --- | --- |
| E1 | `scripts/build-curriculum-seed.ts:20` hardcodes `const partial = true` | The documented `--partial` flag is dead (`process.argv` is never read), so **nothing enforces stage completeness before seeding**. `curriculumTracks.test.ts:90` also passes `allowPartial: true`, and `curriculumSeed.test.ts` only compares the migration against output generated in partial mode. |
| E2 | `modelRegistry.test.ts:225-233` | Scans only for literals already in `MODEL_IDS`, so an id that is *not* in the registry cannot be caught — which is the whole rule. Three live hardcodes pass: `analyze-gulf-arabic/index.ts:1405` (`Fanar-Shaheen-MT-1`), `_shared/curriculumModels.ts:66`, `realtime-session-token/index.ts:494`. The one allow-list entry is *not* stale. |
| E3 | `schemaContract.test.ts:38-47` | Still pins five tables as "these exist in production but no migration creates them". `20260904120000_out_of_band_tables.sql` now creates all five, and `KNOWN_MISSING_TABLES` is empty. Two guards now contradict each other, and the stale one reads first. |
| E4 | Lint baseline, three values | CLAUDE.md says 530; `scripts/lint-ratchet.mjs:26` is **526**; the comment at `:25` says "546 → 545". Anyone lowering it from CLAUDE.md would raise the ratchet. |
| E5 | `docs/testing.md` | "the **84** edge functions" (119 dirs); "**7 of 137** migrations" (7 of 242); names three tables as untracked that now have migrations. |
| E6 | `QA_MAP.md` | Generated 2026-09-04 against 227 migrations; 242 now. Marks `discover-feed` and three anon-capped functions **UNGUARDED** — all four are wrong today — and carries `persist-video-thumbnail` **NOT DEPLOYED** on ~9 routes (see A2). Regenerate with `node qa/build-map.mjs`. |
| E7 | `docs/qa-fix-plan-2026-09-04.md` | Opens "Nothing here is implemented yet", then nine of eleven packages say "done on this branch". |
| E8 | 33 unreasoned `react-hooks/exhaustive-deps` suppressions | Of 37 `eslint-disable` lines, 33 are this rule, concentrated in the audio and timer code (`DiscoverVideo.tsx:704,856`, `Learn.tsx:146,211`, `Transcribe.tsx:332,587`, `QuizCard.tsx` ×3). Only two carry a written reason. D3 is this class of bug. |
| E9 | `src/contexts/` and `src/data/` have no coverage guard | `libCoverage`/`hookCoverage` cover `src/lib` and `src/hooks` only. `TranscriptionJobContext` is untested and can never complete — `startJob` is its only writer and sets `running`; nothing sets `complete`, so `consumeResult` always returns null and the banner persists until `clearJob`. |

Also worth knowing about the coverage guards generally: they are satisfied by
a module's **name appearing** in some test file. Three `src/lib` modules are
matched only incidentally by unrelated tests — `mistakes` (5 KB of
learner-error classification feeding `learner_errors`), `surfaces` and
`dialectAccent`. CLAUDE.md is explicit that this shallowness is deliberate
and that depth is review's job; it is recorded here as a known blind spot,
not as a defect in the design.

---

## Things checked that turned out fine

Recorded so the next pass does not re-litigate them.

- **CORS is clean.** All 121 functions import `_shared/cors.ts`; no
  hand-rolled headers and no `'*'` outside a comment. The Lovable-preview
  wildcard is env-gated behind `ALLOW_PREVIEW_ORIGINS` and off by default.
- **Client↔function names all resolve.** All 91 distinct
  `functions.invoke("…")` names in `src/` and `e2e/` map to a real directory.
- **Column-level types drift is zero.** A full replay of all 242 migrations
  produces 1444 columns across 121 typed tables; the only omissions are the
  four `fanar_usage` columns, which is exactly the allow-list. The
  curriculum-track columns that were lost four times are all present.
- **No RLS holes of the classic kind.** Every table created by a migration
  enables RLS; every `USING (true)` is on shared content or `service_role`;
  every non-`auth.uid()` write policy is keyed on `is_admin()`,
  `can_manage_content()` or `is_recorder()`.
- **Shared-secret gates fail closed** (`_shared/requireRole.ts:101-105`), and
  voice spend is properly metered, including clamping the client-reported
  duration.
- `feedback-screenshots` and `video-audio` — QA_MAP's two storage flags — are
  both fine now; the latter routes through `discover-video-audio`.
- **Migration replay reproduces exactly the 7 pinned files**, nothing new in
  the 15 migrations added since.
- The FSRS-6 formulas match fsrs-rs line for line, including the same-day
  model, the post-lapse cap and the `w20` decay; `weights` and
  `calibrationMultiplier` are genuinely plumbed through all five call sites.
  D1/D2 are the interval, not the memory model.

---

## Suggested order

1. **A1 and A3.** Until deploys and the staleness marker work, nothing
   shipped can be trusted to be live, and every later fix inherits the
   problem. A1's remedy is an ops decision (set the token, or make the manual
   step explicit and checked); A3's is a one-line bump plus widening the
   probe beyond a single function and beyond `canManage`.
2. **B3.** Live personal audio, enumerable with a key from the public bundle.
3. **B2.** Three uncapped paid functions, one of which pays before it
   authorizes.
4. **C1–C3, C6.** All learner-facing, all small, and C1/C2 are one
   `SECURITY DEFINER` helper between them.
5. **D1.** A card that can never leave the queue is a broken core loop.
6. **A6.** Apply the migration to the project, or the feature merged
   yesterday stays half-dead and takes other fields' audit trail with it.

Items in E cost little and are what keep this list from having to be
rediscovered.

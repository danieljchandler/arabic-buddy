project_id = "ovscskaijvclaxelkdyf"

# ─── Functions that require JWT authentication ────────────────────────────────
# Payment / subscription functions — MUST verify JWT
[functions.check-subscription]
verify_jwt = true

[functions.create-checkout]
verify_jwt = true

[functions.customer-portal]
verify_jwt = true

# Admin / pipeline functions — MUST verify JWT.
# Called from authed admin flows (user JWT) and server-to-server from
# extract-visual-context (service-role key — a valid JWT), so requiring a
# valid JWT here does not break the pipeline.
[functions.process-approved-video]
verify_jwt = true

# Re-reads a stored video's on-screen text on demand. Admin-gated inside (or an
# internal service-role call), and the token is read from the Authorization
# header, so a valid JWT is required to reach it at all.
[functions.reextract-on-screen-text]
verify_jwt = true

[functions.curriculum-chat]
verify_jwt = true

# User-data functions — require authentication
[functions.generate-flashcard-image]
verify_jwt = true

# Transcribes and stores the caller's own monologue attempt; enforceDailyCap
# inside is the authorization, and anonymous callers have nothing to store.
[functions.score-monologue]
verify_jwt = true

[functions.generate-story]
verify_jwt = true

# Admin-driven cover illustration for one interactive story. Generates an
# image through the Lovable gateway and writes interactive_stories.cover_image_url.
# JWT is verified and a content-manager role is checked inside, but the token
# is read from the Authorization header, so require a valid JWT here too.
[functions.generate-story-cover]
verify_jwt = false

# ─── Public learning content functions ────────────────────────────────────────
# These serve public learning content or lightweight AI helpers.
# They rely on the Supabase anon key (apikey header) rather than user JWTs.
[functions.deepgram-transcribe]
verify_jwt = false

[functions.analyze-gulf-arabic]
verify_jwt = false

[functions.download-media]
verify_jwt = false

[functions.munsit-transcribe]
verify_jwt = false

[functions.elevenlabs-tts]
verify_jwt = false

[functions.classify-tutor-segments]
verify_jwt = false

[functions.analyze-meme]
verify_jwt = false

[functions.scrape-x-post]
verify_jwt = false

[functions.how-do-i-say]
verify_jwt = false

[functions.fanar-transcribe]
verify_jwt = false

[functions.hf-chat]
verify_jwt = false

[functions.farasa]
verify_jwt = false

[functions.culture-guide]
verify_jwt = false

[functions.translate-phrase]
verify_jwt = false

[functions.discover-trending-videos]
verify_jwt = false

[functions.conversation-practice]
verify_jwt = false

[functions.azure-pronunciation]
verify_jwt = false

[functions.score-shadow-attempt]
verify_jwt = false

[functions.pronunciation-feedback]
verify_jwt = false

[functions.listening-quiz]
verify_jwt = false

[functions.dialect-compare]
verify_jwt = false

[functions.reading-passage]
verify_jwt = false

[functions.daily-challenge]
verify_jwt = false

[functions.grammar-drill]
verify_jwt = false

[functions.azure-tts]
verify_jwt = false

[functions.camel-analyze]
verify_jwt = false

[functions.extract-visual-context]
verify_jwt = false

[functions.soniox-transcribe]
verify_jwt = false

[functions.munsit-tts]
verify_jwt = false

[functions.tts-speak]
verify_jwt = false

[functions.souq-news]
verify_jwt = false

[functions.ask-translation]
verify_jwt = false

[functions.assistant-chat]
verify_jwt = false

[functions.ai-resegment-transcript]
verify_jwt = true

[functions.resync-transcript-timing]
verify_jwt = true

[functions.extract-concepts]
verify_jwt = true

[functions.extract-grammar-points]
verify_jwt = true

[functions.free-chat]
verify_jwt = false

[functions.draft-dialect-rules]
verify_jwt = true

[functions.mine-dialect-corpus]
verify_jwt = true

[functions.dialect-violations-digest]
verify_jwt = true





[functions.generate-listen-script]
verify_jwt = true

[functions.generate-listen-line-audio]
verify_jwt = true

[functions.generate-listen-audio]
verify_jwt = false

# Writes the caller's own grammar mastery. Stated explicitly rather than relying
# on the platform default — this one writes learner data, so the requirement
# should be visible here as well as enforced in the function.
[functions.record-grammar-outcome]
verify_jwt = true

# Captures transcript corrections into training_examples before the admin save
# overwrites them. Role-gated inside (admin / content_reviewer only).
[functions.record-transcript-corrections]
verify_jwt = true

# Learner reports (thumbs-down, "this looks wrong") into the native-review
# queue. Capped per day; reports only become training data after a native
# speaker corrects them.
[functions.report-content]
verify_jwt = true

# Populates the semantic content index (pgvector). Admin-gated inside — each
# run embeds a bounded slice; loop until it reports embedded: 0.
[functions.embed-content]
verify_jwt = true

# Learner referral codes: get-or-create my code, redeem someone else's.
# Rewards are Stripe-side, conversion-gated (see check-subscription).
[functions.referral]
verify_jwt = true

# Paid native feedback: credit ledger, submissions into the reviewers' queue,
# Stripe one-off credit packs (dark until STRIPE_FEEDBACK_PACK_PRICE is set).
[functions.native-feedback]
verify_jwt = true

# Written production (C3): dialect writing prompts and Rulebook-corrected
# reviews of the learner's typed Arabic. Tier-capped inside.
[functions.writing-coach]
verify_jwt = true

# Nightly batch that pre-builds daily stories for recently-active learners.
# Run on a schedule shortly after 00:00 UTC with the service-role key (a
# valid JWT, so verify_jwt = true does not block the scheduler); also
# runnable by an admin. Guarded inside as well.
[functions.pregenerate-daily]
verify_jwt = true

# AI triage for content shared into the app (Web Share Target → /share page).
# One cheap forced-tool-call classification; usage-capped inside, and the
# /share flow only runs signed in, so requiring a JWT costs nothing.
[functions.screen-shared-content]
verify_jwt = true

# One-step admin ingestion of a shared TikTok/YouTube/Instagram link into the
# Discover pipeline. Admin-role-checked inside; kicks process-approved-video
# server-to-server with the service-role key.
[functions.ingest-shared-video]
verify_jwt = true

# Clip-pipeline mining: expands a concept's dialect realizations into caption
# index searches and writes pending clip_candidates. Content-manager-role
# checked inside; the automation loop calls with the service-role key (a valid
# JWT, so verify_jwt = true does not block it) plus the CLIP_PIPELINE_SECRET
# header.
[functions.mine-clip-candidates]
verify_jwt = true

# Clip-pipeline verification stack (term/markers/playability/LLM judge) that
# tiers candidates into verified / needs_review / rejected. Same gating story
# as mine-clip-candidates.
[functions.verify-clip-candidate]
verify_jwt = true

# Clip-pipeline realization drafting: one askBrain call drafts per-dialect
# surface forms + spelling variants into concept_realizations as drafts for
# the native-review lane. Content-manager-role checked inside; automation
# calls with the service-role key + CLIP_PIPELINE_SECRET header.
[functions.draft-concept-realizations]
verify_jwt = true

# Clip-pipeline publication: verified clip candidates onto the learner-facing
# published_clips surface, with a publish-time translation/transliteration
# call. Same gating story as the rest of the pipeline.
[functions.publish-verified-clips]
verify_jwt = true

# Clip-pipeline no-terminal harvest: uploads-playlist enumeration via the
# official Data API, a couple of channels per invoke, driven by a button on
# /admin/channels. Content-manager-role checked inside; automation calls with
# the service-role key + CLIP_PIPELINE_SECRET header.
[functions.harvest-channel-videos]
verify_jwt = true

# Clip-pipeline no-terminal caption indexing via the Supadata transcript API
# (SUPADATA_API_KEY secret) — the cloud-friendly alternative to running
# scripts/fetch-captions.ts locally. Same gating story.
[functions.index-channel-captions]
verify_jwt = true

# Native-speaker transcript review: the only write path a `transcriber` has.
# Declared rather than left to inherit the default, because the whole design
# rests on the caller's identity — `changed_by` on every revision row and
# `reviewed_by` on every checkmark come from this JWT, not from the body.
[functions.transcript-review]
verify_jwt = true

# Placement quiz is the landing page's own CTA — a full model generation that
# must run signed out. Anonymous callers are IP-bucketed per day by
# enforceAnonymousDailyCap in _shared/usageCap.ts, so the anon key alone is
# enough to reach it; a JWT requirement would block the onboarding funnel.
[functions.placement-quiz]
verify_jwt = false

# Set-phrase voice scorer: paid ASR on a public practice page, usable signed
# out. Anonymous callers are IP-bucketed per day by enforceAnonymousDailyCap.
[functions.score-set-phrase-voice]
verify_jwt = false

# Social trends harvester: scrapes free sources (trend aggregators via Jina,
# t.me channel previews, Reddit's registered-app API) and screens every post
# through askBrain before learners can read it. No JWT requirement because the
# scheduled automation loop calls it with the SOCIAL_HARVEST_SECRET header;
# human callers are content-manager-role checked inside.
[functions.harvest-social-trends]
verify_jwt = false
